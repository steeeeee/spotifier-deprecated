import React from 'react';
import SongsList from '../containers/songs_list';

const App = () => {
    return (
        <div className="parent-wrapper">
            <SongsList />
        </div>
    );
};

export default App;
